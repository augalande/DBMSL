create table library (
    id int auto_increment primary key,
    name varchar(255),
    price int );

create table audit (
    id int auto_increment primary key,
    name varchar(255),
    price int );

